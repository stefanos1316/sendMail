To run SOAR:

If you run from docker, first, execute:

1. docker build -t soar .
2. docker run -it -v (local_path)/findings:/soar/findings --rm soar

Then, execute the following commands. If you run from ubuntu 16.4 execute, directly (i.e., we do not need the two previous commands):

1. mkdir soar (if it does not exist)
2. cd soar
3. git clone https://github.com/danieltrt/SOAR.git
4. cd SOAR

Inside SOAR, execute the following commands to setup the environment for executing the experiments.

apt install software-properties-common

add-apt-repository ppa:deadsnakes/ppa

apt-get update

apt install python3.8

update-alternatives --remove python /usr/bin/python3

update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.8 10

python3 --version -> It will give: Python 3.8.8

curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

apt install python3.8-distutils

curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

python3 get-pip.py --force-reinstall

pip3 --version -> It will give the current version of pip3

Check file SOAR/CREATE_ENV.md to create the environment -> To install Anaconda press: bash Anaconda3-2020.11-Linux-x86_64.sh (see https://www.digitalocean.com/community/tutorials/how-to-install-the-anaconda-python-distribution-on-ubuntu-16-04)

ltk.download('averaged_perceptron_tagger') -> nltk.download('averaged_perceptron_tagger')

python3 /soar/SOAR/synthesis/synthesizer/tf_to_torch/torch_synthesizer.py -b conv_for_text -errmsg 
